# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database"

# Server Configuration
PORT=8080
NODE_ENV=production

# Optional: API Keys (if needed for external services)
# PLATZI_API_KEY=your_api_key_here
